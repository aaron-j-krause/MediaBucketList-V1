'use strict';

var React = require('react');

module.exports = React.createClass({
  render: function() {
    /* jshint ignore:start */
    return (
        <a href="/auth/twitter">Sign In With Twitter</a>
    );
    /* jshint ignore:end */
  }
});
